# Kavopay API

Copy-paste examples for the Kavopay public API. One folder per endpoint, each with its own README covering auth, errors, and usage.

## Endpoints

- [`Balance_check`](./Balance_check) — `GET /v1/balance`, returns the current wallet balance
